/* global describe */

import testFunc from '{{pathPrefix}}/storybook.test.js';
import '{{pathPrefix}}/components/{{componentName}}';

describe('{{componentName}}', () => {
  testFunc('{{componentName}}');
});
